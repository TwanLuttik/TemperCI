# Build output
/bin/
*.exe
*.test
*.out

# Vite dashboard output (make build-ui). Hashed assets + index.html are
# generated; go:embed reads this dir at compile time, not from git.
/internal/webui/dist/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Local env / secrets (never commit)
.env
.env.*
*.pem
/etc/
.host-reinstall-backup*
.host-reinstall-backup/

# Local agent/session scratch
.superpowers/

# Isolated git worktrees
.worktrees/
