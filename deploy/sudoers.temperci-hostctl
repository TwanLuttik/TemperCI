# Allow temperci-control (or a dedicated user) to run only temperci-hostctl.
# hostctl may restart units or install the agent (binary + systemd unit).
# Install: copy to /etc/sudoers.d/temperci-hostctl && chmod 440
# temperci ALL=(root) NOPASSWD: /usr/local/bin/temperci-hostctl
