# Allow temperci-control to run only temperci-hostctl (restart/status/install).
# Install: copy to /etc/sudoers.d/temperci-hostctl && chmod 440
# Requires NoNewPrivileges=false on temperci-control.service so sudo can work.
temperci ALL=(root) NOPASSWD: /usr/local/bin/temperci-hostctl
